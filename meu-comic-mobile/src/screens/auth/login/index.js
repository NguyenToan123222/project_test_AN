import React from 'react';

import LoginContainer from './loginContainer';

export default function LoginScreen() {
  const loginScreenProps = {};
  return <LoginContainer {...loginScreenProps} />;
}
